# Streamlit Community Cloud install manifest.
#
# zipper (the generic schema-reconciliation engine this project extends) is
# installed straight from its own public repo, pinned to the exact commit this
# app was verified against. Everything else is pinned to the versions the app
# was built and tested on, so the hosted build reproduces local behavior.
#
# The Google Drive / Dropbox connectors are intentionally omitted: without
# OAuth secrets the Import page shows their setup instructions and the demo
# runs on computer upload + the bundled synthetic files.

zipper @ git+https://github.com/jacksonshuey/zipper.git@fcc33532f44df4b467e834c9737474dde638d637

streamlit==1.57.0
pandas==3.0.3
plotly==6.7.0
pydantic==2.13.4
anthropic==0.104.1
python-dotenv==1.2.2
